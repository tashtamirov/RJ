/** @type {import('next').NextConfig} */
const nextConfig = {}

module.exports = { 
    nextConfig,
    images: {
        domains: ['bryansktoday.ru', 'google.com', 'techwireasia.com']
        
    }
}
